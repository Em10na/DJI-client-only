# Configuration Netlify — Next.js (App Router)
# Le runtime Next.js de Netlify est installé automatiquement via le plugin.

[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
